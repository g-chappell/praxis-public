{
  "name": "praxis",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local, single-user workspace for building apps with an AI coding agent",
  "license": "MIT",
  "packageManager": "pnpm@9.15.5",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "vitest run --passWithNoTests",
    "lint": "prettier --check . && eslint .",
    "format": "prettier --write . && eslint . --fix",
    "build": "pnpm -r --if-present build",
    "dev": "pnpm -r --parallel --if-present dev",
    "db:push": "pnpm --filter @praxis/db db:push",
    "db:seed": "pnpm --filter @praxis/db db:seed",
    "db:codegen": "pnpm --filter @praxis/db db:codegen"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@types/node": "^20.17.6",
    "eslint": "^9.15.0",
    "globals": "^15.12.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0",
    "vitest": "^2.1.5"
  }
}

// Schema + types — safe to import anywhere (no runtime connection).
// For the runtime db client, import from '@praxis/db/client' instead.
export * from './schema';
export type * from './generated/types';
